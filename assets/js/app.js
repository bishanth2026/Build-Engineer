let page='dashboard', currentProject=null;
const app=document.getElementById('app');
function shell(){app.innerHTML=`<div class="app"><aside class="sidebar" id="sidebar"><div class="brand">Build<span>Engineer</span> Pro</div><nav class="nav">${[['dashboard','🏠 Dashboard'],['projects','🏗 Projects'],['calculators','📐 Calculators'],['boq','📋 BOQ & Estimate'],['measurements','📏 Measurement Book'],['reports','📝 Site Reports'],['materials','🧱 Materials'],['labour','👷 Labour'],['tasks','✅ Tasks'],['documents','📁 Documents'],['settings','⚙ Settings']].map(x=>`<button class="${page===x[0]?'active':''}" onclick="navigate('${x[0]}')">${x[1]}</button>`).join('')}</nav></aside><main class="main"><header class="topbar"><div><button class="menu" onclick="toggleMenu()">☰</button><strong>${pageTitle()}</strong></div><div class="toolbar"><span class="pill">${DB.projects.length} projects</span></div></header><div class="content">${renderPage()}</div></main></div>`}
function pageTitle(){return ({dashboard:'Dashboard',projects:'Projects',calculators:'Engineering Calculators',boq:'BOQ & Estimate',measurements:'Digital Measurement Book',reports:'Daily Site Reports',materials:'Material Inventory',labour:'Labour Attendance',tasks:'Tasks',documents:'Documents',settings:'Settings'})[page]||'BuildEngineer Pro'}
function navigate(p){page=p;document.getElementById('sidebar')?.classList.remove('open');shell()}
function toggleMenu(){document.getElementById('sidebar')?.classList.toggle('open')}
function renderPage(){switch(page){case'dashboard':return dashboard();case'projects':return projects();case'calculators':return calculators();case'boq':return boq();case'measurements':return measurements();case'reports':return reports();case'materials':return materials();case'labour':return labour();case'tasks':return tasks();case'documents':return documents();case'settings':return settings();default:return dashboard()}}
function projectSelect(){return `<select id="projectFilter"><option value="">All projects</option>${DB.projects.map(p=>`<option value="${p.id}" ${currentProject===p.id?'selected':''}>${esc(p.name)}</option>`).join('')}</select>`}
function dashboard(){const total=DB.projects.length, open=DB.tasks.filter(t=>t.status!=='Completed').length, low=DB.materials.filter(m=>(m.stock||0)<=(m.min||0)).length, calc=DB.calculations.length;return `<div class="grid cards"><div class="card"><div class="kpi">Projects</div><div class="stat">${total}</div></div><div class="card"><div class="kpi">Open Tasks</div><div class="stat">${open}</div></div><div class="card"><div class="kpi">Low Stock</div><div class="stat">${low}</div></div><div class="card"><div class="kpi">Saved Calculations</div><div class="stat">${calc}</div></div></div><div class="section grid project-grid">${DB.projects.length?DB.projects.map(projectCard).join(''):`<div class="card empty full">No projects yet. Create your first project.</div>`}</div><div class="section"><div class="section-head"><h2>Quick Actions</h2></div><div class="actions"><button class="btn primary" onclick="openProjectModal()">+ New Project</button><button class="btn" onclick="navigate('calculators')">Open Calculator</button><button class="btn" onclick="navigate('reports')">Daily Site Report</button><button class="btn" onclick="navigate('measurements')">New Measurement</button></div></div>`}
function projectCard(p){const prog=projectProgress(p.id);return `<div class="card project-card" onclick="selectProject('${p.id}')"><div class="section-head"><h3>${esc(p.name)}</h3><span class="pill">${esc(p.type||'Project')}</span></div><div class="muted">${esc(p.client||'No client')} · ${esc(p.location||'')}</div><div style="margin-top:14px" class="progress"><i style="width:${prog}%"></i></div><div class="section-head" style="margin-top:7px"><span class="kpi">Progress</span><strong>${prog}%</strong></div><div class="actions"><button class="btn small" onclick="event.stopPropagation();editProject('${p.id}')">Edit</button><button class="btn small danger" onclick="event.stopPropagation();deleteProject('${p.id}')">Delete</button></div></div>`}
function selectProject(id){currentProject=id;page='projects';shell()}
function projectProgress(id){const items=DB.boq.filter(x=>x.projectId===id);if(!items.length)return Number(DB.projects.find(p=>p.id===id)?.progress||0);let done=0,total=0;items.forEach(b=>{total+=Number(b.qty||0);done+=Number(b.completedQty||0)});return total?Math.min(100,Math.round(done/total*100)):0}
function projects(){return `<div class="section-head"><div><h2>Projects</h2><div class="muted">${currentProject?`Selected: ${esc(projectName(currentProject))}`:'Create and manage construction projects'}</div></div><button class="btn primary" onclick="openProjectModal()">+ New Project</button></div><div class="grid project-grid">${DB.projects.length?DB.projects.map(projectCard).join(''):`<div class="card empty">No projects created.</div>`}</div>`}
function openProjectModal(id=''){const p=DB.projects.find(x=>x.id===id)||{};document.body.insertAdjacentHTML('beforeend',`<div class="modal show" id="modal"><div class="modal-box"><div class="section-head"><h2>${id?'Edit':'New'} Project</h2><button class="btn" onclick="closeModal()">✕</button></div><form class="form" onsubmit="saveProject(event,'${id}')"><div class="field"><label>Project name *</label><input name="name" required value="${esc(p.name||'')}" placeholder="Villa Project"></div><div class="field"><label>Client</label><input name="client" value="${esc(p.client||'')}"></div><div class="field"><label>Location</label><input name="location" value="${esc(p.location||'')}"></div><div class="field"><label>Project type</label><select name="type"><option ${p.type==='Residential'?'selected':''}>Residential</option><option ${p.type==='Commercial'?'selected':''}>Commercial</option><option ${p.type==='Renovation'?'selected':''}>Renovation</option><option ${!p.type?'selected':''}>Other</option></select></div><div class="field"><label>Start date</label><input type="date" name="start" value="${p.start||''}"></div><div class="field"><label>Expected completion</label><input type="date" name="end" value="${p.end||''}"></div><div class="field full"><label>Notes</label><textarea name="notes">${esc(p.notes||'')}</textarea></div><div class="full actions"><button class="btn primary">Save Project</button><button type="button" class="btn" onclick="closeModal()">Cancel</button></div></form></div></div>`)}
function closeModal(){document.getElementById('modal')?.remove()}
function saveProject(e,id){e.preventDefault();const d=Object.fromEntries(new FormData(e).entries());if(id)Object.assign(DB.projects.find(p=>p.id===id),d);else DB.projects.push({id:uid('prj'),createdAt:new Date().toISOString(),progress:0,...d});saveDB();closeModal();toast('Project saved');shell()}
function editProject(id){openProjectModal(id)}
function deleteProject(id){if(!confirm('Delete this project and its linked records?'))return;['projects','boq','measurements','reports','materialTx','labour','tasks','documents','calculations'].forEach(k=>DB[k]=DB[k].filter(x=>x.id!==id&&x.projectId!==id));saveDB();if(currentProject===id)currentProject=null;toast('Project deleted');shell()}
function calculators(){return `<div class="section-head"><div><h2>Engineering Calculators</h2><div class="muted">Indicative quantities. Verify against drawings/specifications and applicable standards.</div></div>${projectSelect()}</div><div class="grid calc-grid">${Object.entries(CALCS).map(([k,c])=>`<div class="card calc-card" onclick="openCalc('${k}')"><h3>${c.name}</h3><div class="muted">Calculate in ${c.unit}</div></div>`).join('')}</div><div class="section"><div class="section-head"><h3>Recent Calculations</h3></div>${DB.calculations.length?`<div class="table-wrap"><table class="table"><tr><th>Date</th><th>Project</th><th>Calculation</th><th>Quantity</th><th>Actions</th></tr>${DB.calculations.slice().reverse().slice(0,20).map(c=>`<tr><td>${new Date(c.date).toLocaleString()}</td><td>${esc(projectName(c.projectId))}</td><td>${esc(c.name)}</td><td>${Number(c.qty).toFixed(3)} ${esc(c.unit)}</td><td><button class="btn small" onclick="addCalcToBOQ('${c.id}')">+ BOQ</button></td></tr>`).join('')}</table></div>`:`<div class="card empty">No saved calculations.</div>`}</div>`}
function openCalc(key){const c=CALCS[key];const sel=currentProject||'';document.body.insertAdjacentHTML('beforeend',`<div class="modal show" id="modal"><div class="modal-box"><div class="section-head"><h2>${c.name}</h2><button class="btn" onclick="closeModal()">✕</button></div><form class="form" onsubmit="runCalc(event,'${key}')"><div class="field full"><label>Project</label><select name="projectId">${DB.projects.map(p=>`<option value="${p.id}" ${p.id===sel?'selected':''}>${esc(p.name)}</option>`).join('')}</select></div>${c.fields.map(f=>`<div class="field"><label>${f[1]} (${f[2]})</label><input type="number" step="any" min="0" required name="${f[0]}" value="0"></div>`).join('')}<div class="full actions"><button class="btn primary">Calculate & Save</button><button type="button" class="btn" onclick="closeModal()">Cancel</button></div></form></div></div>`)}
function runCalc(e,key){e.preventDefault();const d=Object.fromEntries(new FormData(e).entries()), c=CALCS[key], vals={};c.fields.forEach(f=>vals[f[0]]=Number(d[f[0]]||0));const r=c.calc(vals);DB.calculations.push({id:uid('calc'),projectId:d.projectId||'',name:c.name,unit:c.unit,qty:r.qty,formula:r.formula,date:new Date().toISOString(),inputs:vals});saveDB();closeModal();toast(`Result: ${r.qty.toFixed(3)} ${c.unit}`);shell()}
function addCalcToBOQ(id){const c=DB.calculations.find(x=>x.id===id);if(!c)return;page='boq';shell();setTimeout(()=>openBoqModal('',c),0)}
function boq(){const pid=currentProject||'';const rows=DB.boq.filter(b=>!pid||b.projectId===pid);const sub=rows.reduce((s,b)=>s+Number(b.qty||0)*Number(b.rate||0),0),gst=rows.reduce((s,b)=>s+Number(b.qty||0)*Number(b.rate||0)*Number(b.gst||0)/100,0);return `<div class="section-head"><div><h2>BOQ & Estimate</h2><div class="muted">${pid?esc(projectName(pid)):'All projects'}</div></div><div class="actions"><button class="btn" onclick="window.print()">Print / PDF</button><button class="btn primary" onclick="openBoqModal()">+ Add BOQ Item</button></div></div><div class="table-wrap"><table class="table"><tr><th>Item</th><th>Description</th><th>Qty</th><th>Unit</th><th>Rate</th><th>GST %</th><th>Amount</th><th>Completed</th><th></th></tr>${rows.length?rows.map(b=>`<tr><td>${esc(b.code||'')}</td><td>${esc(b.desc)}</td><td>${Number(b.qty).toFixed(3)}</td><td>${esc(b.unit)}</td><td>${money(b.rate)}</td><td>${b.gst||0}%</td><td>${money(b.qty*b.rate*(1+b.gst/100))}</td><td>${Number(b.completedQty||0).toFixed(3)}</td><td class="nowrap"><button class="btn small" onclick="openBoqModal('${b.id}')">Edit</button> <button class="btn small danger" onclick="deleteBoq('${b.id}')">Delete</button></td></tr>`).join(''):`<tr><td colspan="9" class="empty">No BOQ items.</td></tr>`}</table></div><div class="section metric-row"><div class="card"><div class="kpi">Subtotal</div><strong>${money(sub)}</strong></div><div class="card"><div class="kpi">GST</div><strong>${money(gst)}</strong></div><div class="card"><div class="kpi">Grand Total</div><strong>${money(sub+gst)}</strong></div></div>`}
function openBoqModal(id='',prefill=null){const b=DB.boq.find(x=>x.id===id)||{};const pId=b.projectId||prefill?.projectId||currentProject||DB.projects[0]?.id||'';document.body.insertAdjacentHTML('beforeend',`<div class="modal show" id="modal"><div class="modal-box"><div class="section-head"><h2>${id?'Edit':'Add'} BOQ Item</h2><button class="btn" onclick="closeModal()">✕</button></div>${!DB.projects.length?'<div class="notice">Create a project first.</div>':''}<form class="form" onsubmit="saveBoq(event,'${id}')"><div class="field"><label>Project *</label><select name="projectId" required>${DB.projects.map(p=>`<option value="${p.id}" ${p.id===pId?'selected':''}>${esc(p.name)}</option>`).join('')}</select></div><div class="field"><label>Item code</label><input name="code" value="${esc(b.code||'')}" placeholder="1.01"></div><div class="field full"><label>Description *</label><input name="desc" required value="${esc(b.desc||prefill?.name||'')}"></div><div class="field"><label>Quantity *</label><input name="qty" type="number" step="any" min="0" required value="${b.qty??prefill?.qty??0}"></div><div class="field"><label>Unit *</label><input name="unit" required value="${esc(b.unit||prefill?.unit||'m³')}"></div><div class="field"><label>Rate (₹)</label><input name="rate" type="number" step="any" min="0" value="${b.rate??0}"></div><div class="field"><label>GST %</label><input name="gst" type="number" step="any" min="0" value="${b.gst??18}"></div><div class="field"><label>Completed quantity</label><input name="completedQty" type="number" step="any" min="0" value="${b.completedQty??0}"></div><div class="full actions"><button class="btn primary">Save</button><button type="button" class="btn" onclick="closeModal()">Cancel</button></div></form></div></div>`)}
function saveBoq(e,id){e.preventDefault();const d=Object.fromEntries(new FormData(e).entries());Object.keys(d).forEach(k=>{if(['qty','rate','gst','completedQty'].includes(k))d[k]=Number(d[k]||0)});if(id)Object.assign(DB.boq.find(x=>x.id===id),d);else DB.boq.push({id:uid('boq'),...d});saveDB();closeModal();toast('BOQ saved');shell()}
function deleteBoq(id){if(confirm('Delete BOQ item?')){DB.boq=DB.boq.filter(x=>x.id!==id);saveDB();shell()}}
function measurements(){const pid=currentProject||'';const rows=DB.measurements.filter(m=>!pid||m.projectId===pid);return `<div class="section-head"><div><h2>Digital Measurement Book</h2><div class="muted">Previous + current = cumulative quantity. Unit follows BOQ item.</div></div><div class="actions"><button class="btn" onclick="window.print()">Print / PDF</button><button class="btn primary" onclick="openMeasurementModal()">+ New Measurement</button></div></div><div class="table-wrap"><table class="table"><tr><th>Date</th><th>BOQ / Work</th><th>Location</th><th>Dimensions</th><th>Previous</th><th>Current</th><th>Cumulative</th><th>Rate</th><th>Amount</th><th></th></tr>${rows.length?rows.slice().reverse().map(m=>`<tr><td>${esc(m.date)}</td><td>${esc(m.work)}</td><td>${esc(m.location)}</td><td>${m.length} × ${m.width} × ${m.height} ${esc(m.unit)}</td><td>${m.previous}</td><td>${m.current}</td><td><strong>${m.cumulative}</strong></td><td>${money(m.rate)}</td><td>${money(m.cumulative*m.rate)}</td><td><button class="btn small danger" onclick="deleteMeasurement('${m.id}')">Delete</button></td></tr>`).join(''):`<tr><td colspan="10" class="empty">No measurements.</td></tr>`}</table></div>`}
function openMeasurementModal(){const p=currentProject||DB.projects[0]?.id||'', items=DB.boq.filter(b=>b.projectId===p);document.body.insertAdjacentHTML('beforeend',`<div class="modal show" id="modal"><div class="modal-box"><div class="section-head"><h2>New Measurement</h2><button class="btn" onclick="closeModal()">✕</button></div><form class="form" onsubmit="saveMeasurement(event)"><div class="field full"><label>Project *</label><select name="projectId" onchange="refreshBoqOptions(this.value)" required>${DB.projects.map(x=>`<option value="${x.id}" ${x.id===p?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div><div class="field full"><label>BOQ item / work *</label><select name="boqId" id="measurementBoq" required><option value="">Select</option>${items.map(b=>`<option value="${b.id}">${esc(b.code)} — ${esc(b.desc)} (${b.unit})</option>`).join('')}</select></div><div class="field"><label>Location / floor</label><input name="location" placeholder="Ground floor"></div><div class="field"><label>Date</label><input name="date" type="date" value="${new Date().toISOString().slice(0,10)}"></div><div class="field"><label>Length</label><input name="length" type="number" step="any" min="0" value="0"></div><div class="field"><label>Width</label><input name="width" type="number" step="any" min="0" value="0"></div><div class="field"><label>Height / thickness</label><input name="height" type="number" step="any" min="0" value="0"></div><div class="field"><label>Current quantity</label><input name="current" type="number" step="any" min="0" required value="0"></div><div class="field full"><label>Remarks</label><textarea name="remarks"></textarea></div><div class="full actions"><button class="btn primary">Save Measurement</button><button type="button" class="btn" onclick="closeModal()">Cancel</button></div></form></div></div>`)}
function refreshBoqOptions(pid){const s=document.getElementById('measurementBoq');if(!s)return;s.innerHTML='<option value="">Select</option>'+DB.boq.filter(b=>b.projectId===pid).map(b=>`<option value="${b.id}">${esc(b.code)} — ${esc(b.desc)} (${b.unit})</option>`).join('')}
function saveMeasurement(e){e.preventDefault();const d=Object.fromEntries(new FormData(e).entries()), b=DB.boq.find(x=>x.id===d.boqId);if(!b){toast('Select a BOQ item');return}const previous=DB.measurements.filter(m=>m.boqId===b.id).reduce((s,m)=>s+Number(m.current||0),0);const current=Number(d.current||0), cumulative=previous+current;const m={id:uid('meas'),projectId:d.projectId,boqId:b.id,work:b.desc,unit:b.unit||b.unit,location:d.location,date:d.date,length:Number(d.length||0),width:Number(d.width||0),height:Number(d.height||0),previous,current,cumulative,rate:Number(b.rate||0),remarks:d.remarks};DB.measurements.push(m);b.completedQty=cumulative;saveDB();closeModal();toast('Measurement saved');shell()}
function deleteMeasurement(id){if(confirm('Delete measurement?')){const m=DB.measurements.find(x=>x.id===id);DB.measurements=DB.measurements.filter(x=>x.id!==id);if(m){const b=DB.boq.find(x=>x.id===m.boqId);if(b)b.completedQty=DB.measurements.filter(x=>x.boqId===m.boqId).reduce((s,x)=>s+Number(x.current||0),0)}saveDB();shell()}}
function reports(){const pid=currentProject||'';const rows=DB.reports.filter(r=>!pid||r.projectId===pid);return `<div class="section-head"><div><h2>Daily Site Reports</h2><div class="muted">Record daily progress, labour, weather, issues and photos.</div></div><div class="actions"><button class="btn primary" onclick="openReportModal()">+ New Report</button></div></div><div class="grid">${rows.length?rows.slice().reverse().map(r=>`<div class="card"><div class="section-head"><h3>${esc(r.date)}</h3><span class="pill">${esc(projectName(r.projectId))}</span></div><div><strong>Weather:</strong> ${esc(r.weather||'—')} · <strong>Labour:</strong> ${r.labour||0}</div><p>${esc(r.work||'')}</p>${r.photos?.length?`<div class="photo-list">${r.photos.map(x=>`<img class="photo" src="${x.data}" alt="site photo">`).join('')}</div>`:''}<p class="muted">${esc(r.remarks||'')}</p><div class="actions"><button class="btn small" onclick="printReport('${r.id}')">Print</button><button class="btn small danger" onclick="deleteReport('${r.id}')">Delete</button></div></div>`).join(''):`<div class="card empty">No site reports.</div>`}</div>`}
function openReportModal(){const p=currentProject||DB.projects[0]?.id||'';document.body.insertAdjacentHTML('beforeend',`<div class="modal show" id="modal"><div class="modal-box"><div class="section-head"><h2>Daily Site Report</h2><button class="btn" onclick="closeModal()">✕</button></div><form class="form" onsubmit="saveReport(event)"><div class="field"><label>Project *</label><select name="projectId" required>${DB.projects.map(x=>`<option value="${x.id}" ${x.id===p?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div><div class="field"><label>Date</label><input type="date" name="date" value="${new Date().toISOString().slice(0,10)}"></div><div class="field"><label>Weather</label><input name="weather" placeholder="Sunny / Rainy"></div><div class="field"><label>Total labour</label><input name="labour" type="number" min="0" value="0"></div><div class="field full"><label>Work completed / planned</label><textarea name="work"></textarea></div><div class="field full"><label>Issues / delays</label><textarea name="issues"></textarea></div><div class="field full"><label>Engineer remarks</label><textarea name="remarks"></textarea></div><div class="field full"><label>Site photos</label><input id="reportFiles" type="file" accept="image/*" multiple></div><div class="full actions"><button class="btn primary">Save Report</button><button type="button" class="btn" onclick="closeModal()">Cancel</button></div></form></div></div>`)}
async function saveReport(e){e.preventDefault();const d=Object.fromEntries(new FormData(e).entries()), files=[...document.getElementById('reportFiles').files];const photos=await Promise.all(files.map(fileToData));DB.reports.push({id:uid('rep'),...d,labour:Number(d.labour||0),photos,date:d.date});saveDB();closeModal();toast('Site report saved');shell()}
function fileToData(file){return new Promise(res=>{const r=new FileReader();r.onload=()=>res({name:file.name,data:r.result});r.readAsDataURL(file)})}
function printReport(id){const r=DB.reports.find(x=>x.id===id);if(!r)return;const w=window.open('','_blank');w.document.write(`<html><head><title>Daily Site Report</title><style>body{font-family:Arial;padding:30px}img{width:180px;margin:5px}table{width:100%;border-collapse:collapse}td{border:1px solid #ccc;padding:8px}</style></head><body><h1>Daily Site Report</h1><p><b>Project:</b> ${esc(projectName(r.projectId))}</p><p><b>Date:</b> ${esc(r.date)} &nbsp; <b>Weather:</b> ${esc(r.weather)} &nbsp; <b>Labour:</b> ${r.labour}</p><h3>Work</h3><p>${esc(r.work)}</p><h3>Issues</h3><p>${esc(r.issues)}</p><h3>Remarks</h3><p>${esc(r.remarks)}</p><div>${(r.photos||[]).map(x=>`<img src="${x.data}">`).join('')}</div><script>window.onload=()=>window.print()</script></body></html>`);w.document.close()}
function deleteReport(id){if(confirm('Delete report?')){DB.reports=DB.reports.filter(x=>x.id!==id);saveDB();shell()}}
function materials(){const pid=currentProject||'';const rows=DB.materials.filter(m=>!pid||m.projectId===pid);return `<div class="section-head"><div><h2>Material Inventory</h2><div class="muted">Receipt and usage transactions update stock automatically.</div></div><button class="btn primary" onclick="openMaterialModal()">+ Add Material</button></div><div class="table-wrap"><table class="table"><tr><th>Material</th><th>Unit</th><th>Stock</th><th>Minimum</th><th>Status</th><th>Transactions</th><th></th></tr>${rows.length?rows.map(m=>`<tr><td>${esc(m.name)}</td><td>${esc(m.unit)}</td><td>${Number(m.stock).toFixed(2)}</td><td>${Number(m.min).toFixed(2)}</td><td>${m.stock<=m.min?'<span class="danger-text">LOW</span>':'<span class="success-text">OK</span>'}</td><td><button class="btn small" onclick="openMaterialTx('${m.id}')">Receipt / Usage</button></td><td><button class="btn small danger" onclick="deleteMaterial('${m.id}')">Delete</button></td></tr>`).join(''):`<tr><td colspan="7" class="empty">No materials.</td></tr>`}</table></div>`}
function openMaterialModal(){const p=currentProject||DB.projects[0]?.id||'';document.body.insertAdjacentHTML('beforeend',`<div class="modal show" id="modal"><div class="modal-box"><div class="section-head"><h2>Add Material</h2><button class="btn" onclick="closeModal()">✕</button></div><form class="form" onsubmit="saveMaterial(event)"><div class="field full"><label>Project *</label><select name="projectId" required>${DB.projects.map(x=>`<option value="${x.id}" ${x.id===p?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div><div class="field"><label>Material *</label><input name="name" required placeholder="Cement"></div><div class="field"><label>Unit</label><input name="unit" value="bags"></div><div class="field"><label>Opening stock</label><input name="stock" type="number" step="any" value="0"></div><div class="field"><label>Minimum stock</label><input name="min" type="number" step="any" value="0"></div><div class="full actions"><button class="btn primary">Save</button><button type="button" class="btn" onclick="closeModal()">Cancel</button></div></form></div></div>`)}
function saveMaterial(e){e.preventDefault();const d=Object.fromEntries(new FormData(e).entries());d.stock=Number(d.stock||0);d.min=Number(d.min||0);DB.materials.push({id:uid('mat'),...d});saveDB();closeModal();toast('Material added');shell()}
function openMaterialTx(id){const m=DB.materials.find(x=>x.id===id);document.body.insertAdjacentHTML('beforeend',`<div class="modal show" id="modal"><div class="modal-box"><div class="section-head"><h2>Material Transaction</h2><button class="btn" onclick="closeModal()">✕</button></div><p><strong>${esc(m.name)}</strong> — Current stock: ${m.stock} ${esc(m.unit)}</p><form class="form" onsubmit="saveMaterialTx(event,'${id}')"><div class="field"><label>Type</label><select name="type"><option value="receipt">Receipt (+)</option><option value="usage">Usage (-)</option></select></div><div class="field"><label>Quantity</label><input name="qty" type="number" min="0.01" step="any" required></div><div class="field"><label>Date</label><input name="date" type="date" value="${new Date().toISOString().slice(0,10)}"></div><div class="field"><label>Reference</label><input name="ref" placeholder="Invoice / work"></div><div class="full actions"><button class="btn primary">Save Transaction</button></div></form></div></div>`)}
function saveMaterialTx(e,id){e.preventDefault();const d=Object.fromEntries(new FormData(e).entries()),m=DB.materials.find(x=>x.id===id),q=Number(d.qty||0);if(d.type==='usage'&&q>m.stock){toast('Insufficient stock');return}m.stock+=d.type==='receipt'?q:-q;DB.materialTx.push({id:uid('mtx'),materialId:id,projectId:m.projectId,...d,qty:q});saveDB();closeModal();toast('Stock updated');shell()}
function deleteMaterial(id){if(confirm('Delete material?')){DB.materials=DB.materials.filter(x=>x.id!==id);DB.materialTx=DB.materialTx.filter(x=>x.materialId!==id);saveDB();shell()}}
function labour(){const pid=currentProject||'';const rows=DB.labour.filter(l=>!pid||l.projectId===pid);return `<div class="section-head"><div><h2>Labour Attendance</h2><div class="muted">Track attendance and daily wage cost by trade.</div></div><button class="btn primary" onclick="openLabourModal()">+ Add Attendance</button></div><div class="table-wrap"><table class="table"><tr><th>Date</th><th>Trade</th><th>Workers</th><th>Wage / worker</th><th>Total</th><th>Project</th><th></th></tr>${rows.length?rows.slice().reverse().map(l=>`<tr><td>${esc(l.date)}</td><td>${esc(l.trade)}</td><td>${l.count}</td><td>${money(l.wage)}</td><td>${money(l.count*l.wage)}</td><td>${esc(projectName(l.projectId))}</td><td><button class="btn small danger" onclick="deleteLabour('${l.id}')">Delete</button></td></tr>`).join(''):`<tr><td colspan="7" class="empty">No labour records.</td></tr>`}</table></div>`}
function openLabourModal(){const p=currentProject||DB.projects[0]?.id||'';document.body.insertAdjacentHTML('beforeend',`<div class="modal show" id="modal"><div class="modal-box"><div class="section-head"><h2>Labour Attendance</h2><button class="btn" onclick="closeModal()">✕</button></div><form class="form" onsubmit="saveLabour(event)"><div class="field"><label>Project *</label><select name="projectId" required>${DB.projects.map(x=>`<option value="${x.id}" ${x.id===p?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div><div class="field"><label>Date</label><input type="date" name="date" value="${new Date().toISOString().slice(0,10)}"></div><div class="field"><label>Trade</label><select name="trade"><option>Mason</option><option>Helper</option><option>Carpenter</option><option>Electrician</option><option>Plumber</option><option>Painter</option><option>Other</option></select></div><div class="field"><label>Workers</label><input name="count" type="number" min="0" value="0"></div><div class="field"><label>Wage / worker</label><input name="wage" type="number" min="0" step="any" value="0"></div><div class="full actions"><button class="btn primary">Save</button></div></form></div></div>`)}
function saveLabour(e){e.preventDefault();const d=Object.fromEntries(new FormData(e).entries());d.count=Number(d.count||0);d.wage=Number(d.wage||0);DB.labour.push({id:uid('lab'),...d});saveDB();closeModal();toast('Attendance saved');shell()}
function deleteLabour(id){if(confirm('Delete attendance?')){DB.labour=DB.labour.filter(x=>x.id!==id);saveDB();shell()}}
function tasks(){const pid=currentProject||'';const rows=DB.tasks.filter(t=>!pid||t.projectId===pid);return `<div class="section-head"><div><h2>Tasks</h2><div class="muted">Assign and track site activities.</div></div><button class="btn primary" onclick="openTaskModal()">+ New Task</button></div><div class="grid project-grid">${rows.length?rows.map(t=>`<div class="card"><div class="section-head"><h3>${esc(t.title)}</h3><span class="pill">${esc(t.priority)}</span></div><div class="muted">${esc(projectName(t.projectId))} · Due ${esc(t.due||'—')}</div><p>${esc(t.notes||'')}</p><div class="actions"><button class="btn small" onclick="toggleTask('${t.id}')">${t.status==='Completed'?'Reopen':'Complete'}</button><button class="btn small danger" onclick="deleteTask('${t.id}')">Delete</button></div></div>`).join(''):`<div class="card empty">No tasks.</div>`}</div>`}
function openTaskModal(){const p=currentProject||DB.projects[0]?.id||'';document.body.insertAdjacentHTML('beforeend',`<div class="modal show" id="modal"><div class="modal-box"><div class="section-head"><h2>New Task</h2><button class="btn" onclick="closeModal()">✕</button></div><form class="form" onsubmit="saveTask(event)"><div class="field full"><label>Project *</label><select name="projectId" required>${DB.projects.map(x=>`<option value="${x.id}" ${x.id===p?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div><div class="field full"><label>Task *</label><input name="title" required></div><div class="field"><label>Priority</label><select name="priority"><option>High</option><option selected>Medium</option><option>Low</option></select></div><div class="field"><label>Due date</label><input name="due" type="date"></div><div class="field full"><label>Notes</label><textarea name="notes"></textarea></div><div class="full actions"><button class="btn primary">Save</button></div></form></div></div>`)}
function saveTask(e){e.preventDefault();DB.tasks.push({id:uid('task'),...Object.fromEntries(new FormData(e).entries()),status:'Pending'});saveDB();closeModal();shell();toast('Task added')}
function toggleTask(id){const t=DB.tasks.find(x=>x.id===id);t.status=t.status==='Completed'?'Pending':'Completed';saveDB();shell()}
function deleteTask(id){if(confirm('Delete task?')){DB.tasks=DB.tasks.filter(x=>x.id!==id);saveDB();shell()}}
function documents(){const pid=currentProject||'';const rows=DB.documents.filter(d=>!pid||d.projectId===pid);return `<div class="section-head"><div><h2>Documents & Drawings</h2><div class="muted">Phase 3.1 stores the actual selected files locally using IndexedDB-style browser storage via data URLs for testing.</div></div><button class="btn primary" onclick="pickDocuments()">+ Add Files</button></div><div class="table-wrap"><table class="table"><tr><th>Name</th><th>Project</th><th>Type</th><th>Size</th><th>Date</th><th></th></tr>${rows.length?rows.slice().reverse().map(d=>`<tr><td>${esc(d.name)}</td><td>${esc(projectName(d.projectId))}</td><td>${esc(d.type||'')}</td><td>${Math.round(d.size/1024)} KB</td><td>${new Date(d.date).toLocaleString()}</td><td><button class="btn small" onclick="downloadDoc('${d.id}')">Download</button> <button class="btn small danger" onclick="deleteDoc('${d.id}')">Delete</button></td></tr>`).join(''):`<tr><td colspan="6" class="empty">No documents.</td></tr>`}</table></div>`}
function pickDocuments(){if(!DB.projects.length){toast('Create a project first');return}document.getElementById('filePicker').onchange=async e=>{const p=currentProject||DB.projects[0].id;for(const f of e.target.files){const data=await fileToData(f);DB.documents.push({id:uid('doc'),projectId:p,name:f.name,type:f.type,size:f.size,date:new Date().toISOString(),data:data.data})}saveDB();toast(`${e.target.files.length} file(s) added`);e.target.value='';shell()};document.getElementById('filePicker').click()}
function downloadDoc(id){const d=DB.documents.find(x=>x.id===id);if(!d)return;const a=document.createElement('a');a.href=d.data;a.download=d.name;a.click()}
function deleteDoc(id){if(confirm('Delete document?')){DB.documents=DB.documents.filter(x=>x.id!==id);saveDB();shell()}}
function settings(){return `<div class="grid cards"><div class="card"><h3>Data Backup</h3><p class="muted">Download all local application data as JSON.</p><button class="btn primary" onclick="exportDB()">Export JSON</button></div><div class="card"><h3>Reset Data</h3><p class="muted">Deletes all local projects, calculations, reports and records from this browser.</p><button class="btn danger" onclick="resetDB()">Reset Everything</button></div><div class="card"><h3>Storage</h3><p class="muted">${JSON.stringify(DB).length.toLocaleString()} characters stored locally.</p></div><div class="card"><h3>Version</h3><p class="muted">BuildEngineer Pro Phase 3.1</p></div></div>`}
function resetDB(){if(confirm('This will permanently remove all local data. Continue?')){DB=JSON.parse(JSON.stringify(DEFAULT));saveDB();currentProject=null;toast('All local data reset');shell()}}
shell();
